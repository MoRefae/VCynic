"use client"; import { useState } from "react"; import Link from "next/link"; import { Footer, Header } from "../ui";
export default function Contact(){const [sent,setSent]=useState(false),[error,setError]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),topic:f.get('topic'),message:f.get('message'),consent:f.get('consent')==='on'})});if(r.ok)setSent(true);else setError((await r.json()).error)} return <main><Header/><section className="contact"><div><div className="eyebrow">CONTACT</div><h1>Get in<br/><i>touch.</i></h1><p>Use this form for product or partnership questions. Please do not send pitch decks, personal data, or credentials through this form.</p><a href="mailto:support@vcynic.ai">support@vcynic.ai</a></div><form onSubmit={submit}>{sent?<div className="success"><b>Message received.</b><p>We will use your details only to respond to this enquiry.</p></div>:<><label>Name<input name="name" autoComplete="name" required placeholder="Your name"/></label><label>Email<input name="email" autoComplete="email" required type="email" placeholder="you@company.com"/></label><label>What can we help with?<select name="topic" required defaultValue=""><option value="" disabled>Choose a topic</option><option>Product question</option><option>Portfolio plan</option><option>Partnership</option></select></label><label>Message<textarea name="message" required placeholder="How can we help?"/></label><label className="check"><input name="consent" type="checkbox" required/> <span>I agree that VCynic may use these details to respond, as described in the <Link href="/privacy">Privacy Policy</Link>.</span></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button primary" type="submit">Send enquiry</button></>}</form></section><Footer/></main>}
