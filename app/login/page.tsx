"use client"; import Link from "next/link"; import { useRouter } from "next/navigation";
export default function Login(){const r=useRouter();return <main className="auth"><Link href="/" className="brand"><span className="brand-mark">V</span>VCYNIC</Link><form onSubmit={e=>{e.preventDefault();r.push('/dashboard')}}><div className="eyebrow">PRODUCT PREVIEW</div><h1>Explore the<br/><i>workspace.</i></h1><p>Authentication is not enabled in this preview. Do not enter a password.</p><button className="button primary">Continue to preview</button><p>Need the preview? <Link href="/signup">Open workspace</Link></p></form></main>}
